# MathLock Play — Environment Variables
# Kopyala: cp .env.example .env && nano .env

# Django
DJANGO_SECRET_KEY=change-me-to-a-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,mathlock.com.tr

# PostgreSQL
MATHLOCK_DB_PASSWORD=change-me-strong-password

# Google Play (opsiyonel — in-app purchase doğrulama)
# GOOGLE_PLAY_SERVICE_ACCOUNT_JSON=/app/google-service-account.json
